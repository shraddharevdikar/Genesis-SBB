export interface SkillReference {
  readonly skillId: string;
  readonly uniqueSkillCode: string; // e.g. "SK-ANALYSIS-R"
  readonly skillNameString: string;
  readonly minimumExperienceYearsValue: number;
}
